[package]
name = "swissengine"
version = "0.2.0"
edition = "2021"
description = "SwissIO low-level session, endpoint mapping, and trace primitives"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
